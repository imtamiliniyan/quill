# Project Roadmap: Quill Launch & Validation Strategy

This roadmap details the progression from the initial Reddit validation campaign to a fully automated custom domain launch, plus the feature ideas under consideration beyond the current dictation app.

---

## Phase 1: Idea Validation (Current)
*   **Goal:** Share the concept on niche subreddits (e.g., `r/macapps`, `r/opensource`, `r/productivity`) to gauge interest and drive traffic.
*   **Hosting:** Active on the free Vercel subdomain (`quill-stt.vercel.app`).
*   **Waitlist Capture:** Active. Email and name entries are stored in the Firestore `waitlist` database.
*   **Email Status:** Currently runs in **Demo Mode**. The serverless function logs signup events to the console instead of sending real emails (saving cost and avoiding unverified spam folders).

---

## Phase 2: Traction & Custom Domain Setup (Post-Validation)
Once the Reddit posts get traction and a custom domain is worth investing in:

1.  **Register Domain:** Buy `quillmac.app` or `quillstt.com` via Hostinger or Namecheap.
2.  **Point to Vercel:**
    - Go to Vercel Dashboard → `quill` project → **Settings** → **Domains**.
    - Add the domain.
    - Copy the DNS records Vercel provides:
      - **A record** pointing to Vercel's IP (`76.76.21.21`) for the root domain.
      - **CNAME record** pointing to `cname.vercel-dns.com` for the `www` subdomain.
    - Paste these records into the domain registrar's DNS management panel.

---

## Phase 3: Branded Email Activation
To start sending verified, branded welcome emails to new signups:

1.  **Verify Domain in Brevo:**
    - Log in to the Brevo account.
    - Go to **Senders & IP** → **Domains** → **Add a Domain**.
    - Brevo will provide three **TXT records** (SPF and DKIM verification).
    - Add these TXT records to the domain registrar's DNS panel (this ensures emails arrive in the primary **Inbox** instead of Spam).
2.  **Enable Vercel Environment Variables:**
    - Go to Vercel Dashboard → `quill` project → **Settings** → **Environment Variables**.
    - Add:
      - `BREVO_API_KEY` = *Brevo SMTP API Key*
      - `BREVO_SENDER_EMAIL` = `waitlist@yourdomain.com` (or whatever address is verified)
      - `BREVO_SENDER_NAME` = `Quill`

*Once saved, Vercel will automatically redeploy, and all subsequent signups will instantly receive their welcome email.*

---

## Phase 4: Feature Ideas Under Consideration (Demand-Test First)

Not committed — each of these gets its own waitlist/interest signal before any engineering time goes in, same discipline as Phase 1. If a landing-page teaser for one of these doesn't pull signups, it doesn't get built.

### 4a. Local Grammar & Fluency Checker — ✅ Architecture decided, build in progress
Inspired by [Refine](https://refine.sh) (Mac-only, $38 lifetime, grammar/fluency/rewrite across 20+ apps) and, later, by FluidVoice's "Fluid Intelligence" tier. Ships as a new **`AutoCleanupLevel.localAI`** rung on the existing Style/Auto Cleanup ladder, between `.light` (rules-only, instant) and `.medium` (BYOK cloud):

- **Engine: Apple's MLX** (`mlx-swift`/`mlx-swift-examples`), not Foundation Models or Gemma 3n as originally sketched below — chosen so it runs on any Apple Silicon Mac today, not gated to macOS 26+/Apple-Intelligence-eligible hardware.
- **Model:** `mlx-community/Llama-3.2-3B-Instruct-4bit`, fetched from Hugging Face on first use (~1.8 GB), not bundled with the app.
- **No GPLv3, no borrowed code:** independently written against Apple's public MLX framework and a publicly-hosted model — not FluidVoice's private "Fluid Intelligence" runtime, which isn't visible in FluidVoice's own public (GPLv3) repo at all. Explicit decision, not deferred.
- **Built so far:** `Sources/quill/Style/LocalEnhancer.swift` (MLX load/generate/download/delete), `AutoCleanupLevel.localAI` case in `Sources/quill/Style/AutoCleanup.swift`, `Package.swift` pinned to `mlx-swift-examples 2.21.2` + `mlx-swift 0.21.x` (only mutually-compatible pairing with WhisperKit's `swift-transformers` requirement).
- **✅ Done.** `swift build` verified clean. Settings UI shipped: the `.localAI` row in Style's Auto Cleanup card (`Sources/quill/UI/StyleView.swift`) now downloads/selects/deletes the model with its own progress bar and confirm dialogs, mirroring the transcription models' pattern. Onboarding also offers it once, right after the transcription model finishes (see Phase 5e).
- Keeps the "nothing leaves your Mac" story intact for the free tier — no BYOK/API-key needed for this rung, only for `.medium`.

### 4b. Local Meeting Capture & Transcription
Inspired by Granola's approach — no bot joins the call; it captures system audio + mic locally. Quill already has the on-device STT engine (WhisperKit/Parakeet); this would be a new capture mode feeding the same pipeline, plus notes generation using whichever local LLM 4a lands on.

- **Differentiator:** Granola still sends audio/notes to the cloud for summarization. A fully local capture-through-notes pipeline ("meeting notes that never leave your Mac") would be a real gap in the current market, not just parity.
- **Known hard part — diarization:** separating "who said what" in a multi-speaker system-audio mix is a distinct, heavier on-device ML problem than single-speaker STT. Needs a feasibility spike before it goes on a landing page.
- **Known hard part — recording consent law:** consent requirements vary by jurisdiction (one-party consent in most US states/federal law vs. two-party/all-party consent in ~11-13 states plus some other countries and require every participant to know). Granola's own approach: doesn't force disclosure, but offers an opt-in camera watermark + automated meeting-chat message, and pushes compliance responsibility to the user via ToS ("this is not legal advice"). If Quill builds this, ship the equivalent opt-in notification feature from day one rather than bolting it on later — same pattern, not a novel one.

---

## Phase 5: UX Polish — FluidVoice-Inspired (UI reference only, no code borrowed)

Source: reviewed FluidVoice's own onboarding flow directly (screenshots, not source) for UX ideas worth adapting to Quill's own design language and existing architecture. Tracked one item at a time, in this order:

- [x] **5a. Transparent model download UX.** ✅ Done. Rejected FluidVoice's browsing list and full marketplace, took only the row layout: added a `speed`/`accuracy` 1–5 rating to `TranscriptionModel` (dots, not a fabricated "96%" — there's no real benchmark behind these, just each architecture's documented general characteristics), rendered as a small `DotRating` next to each row in `ModelsSettingsView`. Separately, built `DownloadActivity.swift` — a global signal a persistent bar in `MainView.swift` observes, visible above every sidebar tab during any model download (transcription switch or Local AI). This closed two real, confirmed gaps, not hypothetical ones: the menu-bar model-switch download runs in a user-closable window whose `Task` keeps going after the window closes with nothing left visible; Style's Local AI row stored progress in view-local `@State`, which SwiftUI discards the moment the sidebar leaves Style and comes back, even though the download itself (an actor-owned Task) keeps running the whole time.
- [x] **5b. Mic input level meter.** ✅ Done — with a correction worth recording: the "surfacing again while actively dictating" half already existed before this was picked up. `RecordingOverlay.swift`'s pill (`AudioCapture.onLevel` → `overlay.pushLevel`) has shown a live 6-bar waveform driven by real mic RMS during every dictation since it was built — this item just hadn't been checked off for it. What was actually missing, and what got built here, was the onboarding half: a `MicLevelMeter.swift` live preview next to the Microphone row in `OnboardingView.swift`'s `PermissionsStep`, shown only once that permission is granted, with a "Speak to test" caption. It runs its own short-lived `AudioCapture` instance (samples discarded, RMS only) — safe alongside the real dictation pipeline since that one doesn't start listening until onboarding finishes (`Quill.swift`'s `box == nil` guard). Reuses `RecordingOverlay`'s existing `Waveform` view and extracted `LevelBars` RMS-shaping (pulled out of `OverlayModel` into a shared enum) rather than a second implementation of the same bars.
- [x] **5c. Sequential permission checklist.** ✅ Done, with the Test Playground piece deliberately deferred. `PermissionsStep` in `OnboardingView.swift` is now a real two-step checklist — Accessibility stays visually locked (dimmed, disabled button, "Complete microphone access first") until Microphone is actually granted, each row showing a numbered badge that becomes a checkmark plus an explicit "Needed"/"Ready" label. `Getting Started` ended up as a real fifth **sidebar tab** (`GettingStartedView.swift`, peer to Dictation/Insights/Style/Settings) rather than the Settings-sub-tab compromise first shipped here — closer to how FluidVoice actually keeps it, once there was a real reason to add that to `MainView.swift`'s `SidebarItem` enum. Holds a live status recap of all four setup checks (mic, accessibility, model, Local AI) and a working "Run Onboarding Again" button. This needed real plumbing either way: threaded a `MainWindow.onRunOnboardingAgain` closure through `MainView` (mirroring how `menuBar` already flows through that chain) so it can reach back to the onboarding window `Quill.swift` owns. Caught two real bugs while building this: `OnboardingWindow`'s fixed NSWindow size (480×580) had drifted from `OnboardingView`'s actual height (620, grown for the Local AI step) and was silently clipping content; and naively re-running the onboarding-finished flow on a replay would have called `HotkeyMonitor.start()` a second time, which creates a brand-new `CGEventTap` rather than replacing the old one — confirmed by reading it directly — so every hotkey press would've double-fired, the same failure mode `SingleInstance` exists to prevent across processes. Guarded with `box == nil` so the real first-time setup still runs exactly once. Test Playground intentionally not built yet — it needs the same live-transcript mechanism 5d's overlay builds; a one-off recording flow just for this page would duplicate that work instead of reusing it.
- [ ] **5d. Live dictation overlay ("the pill") — on hold, by choice.** Scoped before starting, per this item's own instruction to plan first: the floating pill, its position, waveform, and recording/transcribing/polishing states already shipped as part of `RecordingOverlay.swift` — built earlier, just not previously checked off here. The one piece of the original description still missing is the live transcript text growing as you speak. Blocked on a real cost tradeoff: `Transcriber.swift`'s protocol only exposes a single batch `transcribe(_ audio:) async throws -> String`, no streaming/partial-result API in either backend (Parakeet or WhisperKit) — approximating "live text" means periodically re-transcribing the whole buffer-so-far (~every 1–1.5s) and swapping in the longer result, which costs extra CPU/battery while recording and can visibly "flicker" as later passes revise earlier guesses. Presented this tradeoff directly rather than picking silently; held off building it for now. FluidVoice's Overlay settings panel (position, size, live-preview on/off, max preview-length) remains a fine v2 template once/if this gets picked back up.
- [x] **5e. Local AI onboarding step.** ✅ Done. New `.localAI` `OnboardingStep`, shown once right after the transcription model finishes downloading (`OnboardingState.swift`, `OnboardingView.swift`'s `LocalAIStep`) — three equally-visible ways out: download & enable, use your own AI key instead (sets Auto Cleanup to `.medium`, key still entered later in Style), or skip for now. Paired with the Settings-side piece from 4a: the `.localAI` row in Style's Auto Cleanup card now has its own confirm-download / progress / delete handling instead of silently flipping the setting with no model behind it. `swift build` verified clean end to end.
- [x] **5f. Toggle activation mode.** ✅ Done. Inspired by FluidVoice's Global Hotkey settings — Quill's hotkey was hold-only (press, speak, release to finish); added a `Click to toggle` alternative so a longer thought doesn't mean physically holding a key down. New `ActivationMode` enum (`Input/ActivationMode.swift`, `.hold` default / `.toggle`), a `QuillSettings.activationMode` setting, and a segmented picker under a new "Dictation" section in `SettingsView.swift`'s `GeneralSettingsView`. `HotkeyMonitor` itself stayed untouched — it only ever emits raw `.pressed`/`.released`, mode-agnostic by design — so the actual behavior split lives in `Quill.swift`'s `attachDictationHandlers`: the old inline `.pressed`/`.released` closures became reusable `beginRecording()`/`finishRecording()` functions, dispatched by `QuillSettings.activationMode` on every hotkey event. `.hold` is byte-for-byte the original behavior (unchanged for existing users' muscle memory); `.toggle` flips a local `isTogglingRecording` flag on press and ignores releases entirely — releasing a tap is near-instant, so wiring `.toggle`'s stop to `.released` the way `.hold` does would end the recording before the user said anything. `swift build` verified clean.
- [x] **5g. Automatic activation mode, Text Formatting, and Voice Engine sidebar tab.** ✅ Done, three FluidVoice-inspired additions in one pass:
  - **Automatic (Both) activation mode.** A third `ActivationMode` case that accepts either gesture on the same hotkey — a quick tap starts a recording a second tap later stops (like Toggle), or a press held past `ActivationMode.automaticHoldThreshold` (0.35s) and released stops right there (like Hold, walkie-talkie style). The two can't be told apart at press time, only resolved on release (or a second press, for the tap case) — implemented in `attachDictationHandlers` with `isAutoRecording`/`autoPressStartedAt`, sibling state to Toggle's `isTogglingRecording`, not a replacement for it.
  - **Primary Dictation Shortcut display.** Quill's hotkey is hardcoded to `fn` (`HotkeyMonitor`'s `.maskSecondaryFn`) — no remapping exists yet, so rather than fake an editable control, `GeneralSettingsView` shows it as a labeled "fn (Globe key) · Fixed for now" row, honest about what's actually configurable. The Activation Mode picker sits directly below it, matching the reference layout.
  - **Text Formatting toggles.** Three of the five shown in the FluidVoice reference, the three actually requested: Lowercase First Letter, Space Between Dictations, Smart Capitalization (Remove Trailing Period and Slash Commands/@ Formatting were in the screenshot but not asked for — left out rather than assumed). New `QuillSettings` bools, applied by a new `TextFormatting.apply(_:)` (`Style/TextFormatting.swift`) — runs last in the pipeline, after Auto Cleanup, right before `TextInjector.inject`. Space Between Dictations and Smart Capitalization both need to know what's already typed before the cursor, which required a new `CursorContext.swift` (`Input/`) — a best-effort Accessibility-API read of the focused element's text before the selection, reusing the same Accessibility permission `HotkeyMonitor` already requires. Deliberately fails to `nil` (not a guessed default) when an app's AX support won't answer — several Electron/Chromium editors don't expose a standards-compliant `AXValue`/`AXSelectedTextRange` — and both toggles treat `nil` as "leave it alone," not "assume start of line." Smart Capitalization takes priority over the blunter Lowercase First Letter when both are on, since it actually reads context and the other doesn't.
  - **Voice Engine sidebar tab.** The Models list (`ModelsSettingsView`, plus its `DotRating` helper from 5a) moved out of Settings' General tab into a new top-level `VoiceEngineView.swift`, a sixth `SidebarItem` peer to Dictation/Insights/Style/Getting Started — mirrors how FluidVoice keeps its own model picker in a dedicated sidebar section rather than buried in general settings (layout reference only; the actual model-switch logic is unchanged, same `MenuBarController.selectModel` entry point).
  
  `swift build` verified clean end to end.

---

## Phase 6: Feature Ideas Surfaced from Settings Reference (not committed — layout/interaction inspiration only)

FluidVoice's full in-app Settings sidebar (Settings / Voice Engine / AI Enhancement / Custom Dictionary / Command Mode / Edit Mode / History / Stats) is a much bigger surface than Quill's current Settings. Sorted into what's worth borrowing vs. explicitly not, per the same "layout only, not color/branding" rule already agreed:

**Cheap, self-contained polish (small, no new product surface):**
- [x] ✅ Done (5g). "Space Between Dictations" and "Smart Capitalization" — correction to this list itself: these shipped as part of 5g's Text Formatting toggles, alongside "Lowercase First Letter." Left un-updated here until now. "Remove Trailing Period" (the one toggle from that original screenshot that wasn't requested) is still genuinely not built.
- [x] ✅ Done (6a). Filler-word list made editable — `QuillSettings.fillerWords` (default: the standard ~17 English filler interjections — um, uh, er, ah, eh, hmm, etc. — not FluidVoice-specific content, just the generic list any dictation tool's defaults would look like) plus a master `removeFillerWords` toggle, both read by `TranscriptSanitizer.cleanUpFillers`. UI lives in Voice Engine: a chip list (add/remove/reset-to-default) under a new `FillerWordsSettingsView`, using a hand-rolled `FlowLayout` (SwiftUI's `Layout` protocol, macOS 14+) since there's no built-in wrapping container. Multi-word phrase fillers ("you know," "sort of," etc.) stay a fixed internal list, not exposed as chips — FluidVoice's own reference is single-word interjections only, and phrase matching doesn't fit the same chip-editing model.
- Input Device Priority (ordered fallback list of mics) and an Output Device picker for sound cues — only relevant once Quill has its own recording-start/stop sound cue, which it may not yet.
- [x] ✅ Done. **Voice Engine model list expanded from 4 to 9.** Added `whisper-tiny.en` (73MB), `whisper-medium.en` (1459MB), `whisper-large-v3` (2947MB), `parakeet-tdt-0.6b-v2` (443MB, English-only, FluidAudio's `.v2` checkpoint — the existing Parakeet entry was implicitly `.v3`, now both are explicit via `TranscriptionModel.parakeetVersion` and `ParakeetModelVersion.swift`'s `asrModelVersion` resolver), and `parakeet-tdt-ctc-110m` (217MB, English-only, FluidAudio's `.tdtCtc110m` — a 110M-param hybrid TDT-CTC model, the closest thing the SDK has to FluidVoice's "Parakeet Flash (Beta)"; labeled by what it verifiably is rather than reusing that name, since there's no way to confirm from here it's the identical NeMo checkpoint). All 5 sizes computed from HuggingFace's tree API, same "real number, not a guess" posture as the existing entries. Caught and fixed a latent bug while wiring the second Parakeet variant: `ParakeetTranscriber.warmUp` and `ModelAvailability`'s isDownloaded/deleteFiles were hardcoded to FluidAudio's `.v3` — harmless with one Parakeet model, would've silently loaded/deleted the wrong one's files with two. Deliberately did **not** add FluidVoice's SenseVoice, Paraformer, or Parakeet-EOU — different architectures Quill's two engines (WhisperKit, FluidAudio) can't run; each would need its own new `Transcriber` implementation, scoped separately, not "add a registry entry."
- **Explicitly scoped out: FluidVoice's "Nemotron 3.5 Multilingual" and "Nemotron Speech 3.5 (Ultra Fast Low Latency)".** Checked FluidAudio's actual source before promising these — real repos exist (`nemotronMultilingual`, `nemotronStreaming{560,1120,2240}ms`), but they're served through `StreamingNemotronAsrManager`/`StreamingNemotronMultilingualAsrManager`, a session-based streaming API (`appendAudio`/`process(buffer:)`/`finish()`, chunk-tiered subpath downloads, partial-transcript callbacks) — structurally nothing like the single-shot `AsrManager.transcribe(audio:)` call `ParakeetTranscriber` wraps today. Not a registry entry; a genuinely new `Transcriber` implementation and its own scoping pass, same category as SenseVoice/Paraformer/EOU above.

**Real new features (bigger scope — each needs its own waitlist/demand signal per the Phase 4 working principle before it gets scoped, same discipline as 4a/4b):**
- [x] ✅ Done (6b), pulled forward ahead of its own demand-signal gate at your explicit request. **API key management as its own sidebar tab.** New `EnhancementEngineView.swift` — a real `SidebarItem` peer to Dictation/Insights/Style/Voice Engine/Getting Started, listing Quill's BYOK providers as expandable rows: click to reveal a key field, a "Get API Key" link to that provider's own key page, Save/Remove/Cancel. No per-provider model picker (except OpenRouter, see below), by explicit instruction — Style still owns that. Replaces the old combined provider-picker + key-field card that lived inline in `StyleView.swift`'s `apiKeyCard`; Style itself keeps only the read-side state (`hasKey`/`provider`) its Medium badge and Rewrite card still need. Saving a key here also sets `QuillSettings.styleProvider` to that provider, preserving exactly what the old combined picker did implicitly.
- [x] ✅ Done. **Expanded BYOK provider list: Google + OpenRouter added**, real API calls (Gemini REST, OpenAI-compatible chat completions), real logo icons (`Sources/quill/Resources/ProviderLogos/`), `StyleProvider` now `{openAI, anthropic, google, openRouter}`. An explicit "Active" badge + "Use for Rewrite & Medium" switch button were added across all 4 rows once four simultaneously-connectable keys made "which one's actually in use" ambiguous. xAI/Groq/Cerebras/Ollama/LM Studio/custom-endpoint remain un-added — no demand signal yet.
- [x] ✅ Done. **OpenRouter model picker** — searchable dropdown over OpenRouter's public models catalog (live-fetched, `OpenRouterModels.swift`), same interaction shape as FluidVoice's own picker (search field, scrollable checkmarked list, "N more — refine your search"), persisted via `QuillSettings.openRouterModel`.
- [x] ✅ Done. **Local AI moved into Enhancement Engine, listed first.** Its download/delete/progress management (previously inline in Style's Auto Cleanup card) now lives in `EnhancementEngineView.swift` as a peer card ahead of OpenAI/Anthropic/Google/OpenRouter — one place to manage every AI backend, on-device or BYOK. Style's Auto Cleanup card still owns *selecting* Local AI as the active tier (same split already true of Medium: the key/model lives in Enhancement Engine, which tier actually runs stays a Style choice) — its row now just shows a "READY"/"NOT DOWNLOADED · USES LIGHT" badge, same pattern as Medium's "USES YOUR KEY" badge.
- [x] ✅ Fixed alongside the above. **Insights' "Fixes Made by Quill" card was stuck on "--" for Google/OpenRouter users.** Its unlock gate (`hasStyleKey` in `InsightsView.swift`) still only checked `.openAI`/`.anthropic` — the same hardcoded-pair bug class as the `StyleView.hasKey` ternary caught earlier, just not caught at the time Google/OpenRouter shipped. Fixed to check all `StyleProvider.allCases`.
- **Custom Dictionary / pronunciation training.** "Teach Words" by voice (repeat-until-3-correct loop) or by typing, auto-learned corrections, import/export. A genuinely new feature area, not a tweak to something existing.
- **Spoken punctuation formatting.** A configurable start word (e.g. "literal") that puts the next spoken word into a formatting/punctuation mode ("literal comma" → ","). New feature, own scope.

- [x] ✅ Done, then upgraded same day. **Change Log tab.** New `ChangeLogView.swift`, a real sidebar peer showing dated release notes. Originally backed by a hand-maintained local array; upgraded once you chose to make the repo public — the quill repo (`github.com/imtamiliniyan/quill`) is now **public**, tagged `v0.1.0`, and the Change Log tab fetches real release notes live from GitHub's public Releases API (`GitHubReleases.swift`, no auth needed, same pattern as `OpenRouterModels.fetch()`), falling back to the local array only if the fetch fails. Verified the full loop: full history scanned for any secret ever committed (clean — confirmed before flipping visibility), 47 files' worth of uncommitted Phase 5-7 work committed and pushed, tagged `v0.1.0`, a real GitHub Release published with honest notes, repo visibility flipped, and the app confirmed fetching it live.
- [x] ✅ Done. **GitHub Sponsors.** Added `.github/FUNDING.yml`; you completed enrollment and the Feedback tab's "Support Quill" button now points at the real, live `github.com/sponsors/imtamiliniyan`.
- [x] ✅ Done. **Real version + release, backdated correctly.** `v0.5.0` is the current tagged release (bumped up from an initial `v0.1.0` that undersold how much was built). `v0.1.0` also exists, tagged on the actual Aug 10, 2026 commit (the real first build), not a placeholder — its git tag object itself is backdated to match. Both releases' notes carry a `Released: yyyy-MM-dd` line since GitHub's Release API can't backdate `published_at`, and the in-app Change Log parses that line instead of trusting raw publish time, so sort order and displayed dates are both correct.
- [x] ✅ Done. **Discovered and fixed a pre-existing, unattended CI publish path.** `.github/workflows/release.yml` (not written this session, already in the repo) builds and publishes a GitHub Release on every `v*` tag push, no approval gate — it fired 3 times unattended while tags were being pushed, each time silently uploading an unsigned, un-notarized CLI-only tarball (not the real signed `.app`/`.dmg`) as a public downloadable asset. Removed those stray assets, uploaded the real signed `Quill.dmg` by hand, and set `draft: true` on the workflow's release step: it still builds automatically on every tag push, but the result stays invisible and undownloadable until manually published from GitHub's UI. A full signed+notarized CI pipeline (so the real installer gets built in the cloud too, not just locally) is a further, bigger step — needs your Developer ID cert and notarization credentials added as GitHub Secrets, which only you can do; flagged to you, not built yet.
- [x] ✅ Done. **Local AI's model name surfaced.** Enhancement Engine's Local AI card was the one row that didn't say which model it actually runs. Added a "Model: Llama 3.2 3B Instruct (4-bit)" line (`LocalEnhancer.displayName`), matching the "Uses gpt-4o-mini" caption every cloud provider row already had.
- [x] ✅ Done. **Em dashes stripped from every app-facing string.** UI text, CLI (`quill setup`/`doctor`) output, and the LLM system prompts sent to providers — all replaced with a period-split or colon, matching the convention already set by the original Aug 11 em-dash cleanup commit. Doc comments left alone (not user-facing).
- [x] ✅ Done. **README rewritten**, referencing FluidVoice's README structure (layout only, all original wording): a real "Download the latest release" link to `/releases/latest`, an updated feature list reflecting Enhancement/Voice Engine, a real supported-models table (9 models, real sizes), and a Privacy section.
- **Noted, not fixed:** `LICENSE` still carries the original "Andrew Jones" copyright from the upstream `parrot` fork this project started from. MIT's terms generally require preserving the original notice on redistributed code, so this isn't a simple find-replace — flagged for you to decide how to handle, not touched.
- [x] ✅ Done. **Feedback tab, scoped down from the FluidVoice reference on explicit instruction.** New `FeedbackView.swift`: a "Loving Quill?" card with one "Support Quill" button (not FluidVoice's two — Star on GitHub + Support — collapsed into one, per instruction) opening the real repo, `https://github.com/imtamiliniyan/quill` (confirmed via `git remote -v`, not guessed). Below it, a real feedback form (email, text, "include debug info" toggle) that opens a pre-filled `mailto:` draft to Quill's actual existing contact address, `tamil@iniyan.pro` (same one already live on the landing site's privacy/terms pages) — genuinely functional with zero new backend, as opposed to a "Send" button that looks real but goes nowhere.

**Explicitly not taking inspiration from:**
- **Command Mode / Edit Mode** — voice-driven Mac control and agentic text editing. Different product entirely from "clean up my dictation," alpha-quality even in FluidVoice, out of scope.
- **Accent Color picker, dark/light theme switcher** — this is exactly the branding/color layer you said to leave out.
- **Rollback / Get Previous Builds / Beta Releases channel** — auto-update infrastructure Quill doesn't have yet; not a UI decision, a distribution decision for later.

---

## Phase 7: Insights & Stats Expansion — Habit/Motivation Layer (FluidVoice-inspired) — ✅ Complete

Purpose stated plainly, to keep this honest as it's built: this is for the *user* to see their own usage and build a habit — not telemetry for us. Everything below is a local computation over `DictationHistory.loadAll()`'s existing entries (`Sources/quill/History/DictationStats.swift`, rendered in `Sources/quill/UI/InsightsView.swift`) — nothing new leaves the Mac, nothing new gets written anywhere except the one setting in Tier 3. No demand-gate needed the way Phase 4/6's bigger bets have one — this is cheap enough to just build.

**Tier 1 — pure computed properties over the existing `entries` array, no new state:** ✅ Done
- [x] Transcriptions count, Avg Length — new fields on `DictationStats`, shown in a "Personal records" card in `InsightsView.swift`.
- [x] Personal Records: Longest Transcription, Most Words in a Day, Most Transcriptions in a Day — same card.
- [x] Best Streak — turned out `StreakCalendarView.swift` already computed this privately as `longestStreak`; consolidated it onto `DictationStats.bestStreak` instead of keeping two copies, and both views now read the one calculation.
- [x] Milestones checklist (words: 1K/10K/50K/100K/500K/1M · transcriptions: 50/100/500/1K/5K/10K · streak: 7/14/30/60/100 days/1 yr) — new card, X/18 reached, streak track uses best-ever streak so breaking a streak doesn't take back an earned milestone.

**Tier 2 — small new aggregations, still no persistence:** ✅ Done
- [x] Activity bar chart, 7-day/30-day toggle (words per day) — new `ActivityChartView.swift`, a sibling to `StreakCalendarView.swift`'s heatmap (answers "how much," not just "did I").
- [x] Peak Time — `DictationStats.peakHour`, a mode over `Calendar.component(.hour:)`, shown as an "18-19"-style range in the new Insights grid.
- [x] AI Enhanced % — a percentage framing of `stats.fixesCount`. While building this, found the existing "unlock" gate (`hasStyleKey`, BYOK-only) had gone stale the moment Local AI shipped: a user on the free, no-key Local AI tier was getting real AI-driven fixes but the card stayed locked as if they weren't. Fixed via `hasAIEnhancement` (BYOK key OR Local AI downloaded+selected), applied to both the new tile and the existing "Fixes Made by Quill" card.

**Tier 3 — needs one new setting:** ✅ Done
- [x] Time Saved, with a user-editable assumed typing WPM (default 40) — `QuillSettings.assumedTypingWPM`, a `Stepper` right on the card. Formula: `(words / WPM) − actual dictation time`, clamped at 0, assumption always shown next to the number. Also powers Today's compact "saved" number, scoped to just today's entries — same formula, not a second one.

**Tier 4 — check before building:** ✅ Turned out to already exist
- [x] "Reset All Stats" — the earlier "doesn't exist anywhere yet" note was wrong, a grep for the wrong terms missed it: `Data & Privacy` already has "Clear history," wired to `DictationHistory.clear()`, which already resets every Insights stat since they're all pure functions of that history. Added the same action as a small link at the bottom of Insights itself too, so it's reachable without a trip to Settings — same function, not a duplicate.

**"Today" card:** ✅ Done — words/time-saved/sessions for today, with a streak badge, sitting above the existing stat row. Presentational grouping of the same Tier 1–3 numbers filtered to `Calendar.current.isDateInToday`, not a new data source.

---

## Working principle carried across all of Phase 4

Ideas surface fast (a Reddit scroll is enough to spawn several) — the filter is always demand first, architecture second. Nothing in 4a/4b gets scoped into an implementation plan until its own waitlist signal justifies it, same discipline that got Quill itself from idea to Phase 1.
