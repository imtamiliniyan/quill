.build/
.swiftpm/
# Compiled MLX Metal shader library (mlx.metallib) — real build artifact,
# not source; see scripts/build-app.sh's "building mlx.metallib" step for
# why it can't be produced by `swift build` alone and has to be cached
# here instead of inside .build/ (which a fresh checkout wipes).
.metallib-cache/
*.xcodeproj
*.xcworkspace
.DS_Store
dist/

# kdb auto-rendered task index (regenerate with `kdb render`)
.tasks/

# Environment variables — NEVER commit real credentials
# .env.example is safe to commit (it contains no real values)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Node
node_modules/

# Firebase Credentials
quill-stt-firebase-adminsdk-fbsvc-5fef275926.json

# Internal roadmap — was tracked and publicly visible on GitHub; kept
# locally for planning, no longer synced to the public repo.
ROADMAP.md